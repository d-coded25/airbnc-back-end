{
  "name": "airbnc",
  "version": "1.0.0",
  "description": "## Project Overview:",
  "main": "listen.js",
  "scripts": {
    "dbs-setup": "psql -f ./db/db-setup.sql",
    "seed-test": "NODE_ENV=test node ./db/seed-run.js",
    "seed-dev": "node ./db/seed-run.js",
    "seed-prod": "NODE_ENV=production node ./db/seed-run",
    "test-utils": "jest utils --watchAll",
    "test-app": "jest app --watchAll",
    "dev": "nodemon listen.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "pg": "^8.16.3",
    "pg-format": "^1.0.4"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.10",
    "supertest": "^7.1.4"
  }
}
